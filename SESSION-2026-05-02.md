# Session 2026-05-02 — News + News Ticker

## งานที่ทำเสร็จ (Production deployed)

### 1. ดึงข่าวจริงจาก DB ทุกที่ที่เคยใช้ dummy
**Commit:** `355f474 fix(news): ดึงข่าวจริงจาก DB ในหน้าหลัก/ticker + เพิ่ม flag เลือกข่าวลง ticker (สูงสุด 5)`

- พบบั๊ก: `HomeMainContent.tsx` query news ใช้ `is_published`, `summary`, `view_count`, `author` ผิดทั้งหมด — schema จริงคือ `published`, `excerpt`, `views` (ไม่มี `author`) → query ล้มเหลวตลอด → fallback เข้า `DUMMY_NEWS` 6 รายการ
- ลบ `DUMMY_NEWS` ทั้งก้อน + photo album guard เมื่อไม่มีรูป
- ลบ `FALLBACK_TITLES` ใน NewsTicker — empty state = ซ่อน section ทั้งหมด
- แก้ `news.service.ts` typo `is_published`→`published` ด้วย

### 2. Migration 031: เลือกข่าวลง ticker ได้สูงสุด 5 ข่าว
**ไฟล์:** `supabase/migrations/031_news_show_in_ticker.sql` (apply แล้วใน production ผ่าน Supabase Dashboard)

```sql
alter table public.news
  add column if not exists show_in_ticker boolean not null default false,
  add column if not exists ticker_order integer;

create index if not exists idx_news_ticker
  on public.news (ticker_order asc nulls last, published_at desc)
  where show_in_ticker = true and published = true;
```

- `useTickerItems`: filter `show_in_ticker = true` เรียงตาม `ticker_order` limit 5
- `NewsForm`: Switch "แสดงในตัววิ่งข่าว" + counter `x/5` + auto-assign `ticker_order` ตอน append
- `NewsList`: badge "📢 ใน ticker"
- ตาราง `ticker_items` เก่ายังอยู่ใน DB แต่ไม่มีโค้ดใช้แล้ว (ลบทีหลังได้)

### 3. Follow-up fixes
**Commit:** `f0fabe3 fix(news): แก้ link จากหน้าแรก/ticker ให้เปิด detail + ลบ Manual ticker UI + UX hint`

- ทุก Link ของข่าวในหน้าแรก + ticker → `/news?id={id}` (ของเดิมใช้ `/news/{id}` path segment ที่ไม่มี route, News.tsx รับผ่าน query string `?id=`)
- ลบ Card "รายการตัววิ่ง (Manual)" ใน `TickerManagement.tsx` ทั้งก้อน — เหลือเฉพาะ Settings (speed/gap/pause)
- เอา manual ticker_items source ออกจาก `useTickerItems` — ใช้แค่ news source
- NewsForm: เพิ่ม hint สีเขียว "✓ ข่าวนี้กำลังวิ่ง — คลิก Switch เพื่อยกเลิก" ตอนเปิด toggle

### 4. Mobile ticker ไม่วิ่ง
**Commit:** `79ddd42 fix(ticker): mobile ticker ไม่วิ่ง — refactor เป็น duplicate items + animate to -50%`

- ของเดิมพึ่ง `ResizeObserver` จับ `containerW` แล้ว inject ใน `<style>` block ใน component → mobile address bar/keyboard shrink → animation restart ตลอด → ดูเหมือนไม่วิ่ง
- ของใหม่: duplicate items 2 ชุด + animate `translate3d(0) → translate3d(-50%)` (loop ลื่น ไม่พึ่ง width)
- ย้าย `@keyframes` ไป `src/index.css` (constant)
- ใช้ `translate3d` (force GPU layer) + เพิ่ม `onTouchStart/End` สำหรับ pause-on-touch

---

## Hard rules ที่ใช้กับ task นี้
- ทุก commit ใช้ pnpm + RTK + Claude co-author footer
- Migration prefix 031 (ตามลำดับ filename ล่าสุด — มี duplicates 026/027 เก่า แต่ unrelated)
- ไม่แตะ component ใน `components/ui/` — ใช้ wrapper component ที่อื่นแทน
- ใช้ `cn()` + CSS vars (bg-primary, text-foreground) ตลอด

---

## งานที่ทำต่อ (TODO)

### High priority
- [ ] **Test บน production จริงทุก device** หลัง deploy `79ddd42`:
  - Mobile (iPhone Safari, Android Chrome): ticker วิ่งจริงไหม, แตะ pause ได้ไหม
  - Click ข่าวจากหน้าแรก/ticker → เปิด detail ของข่าวนั้นจริง (ไม่ใช่หน้ารวม)
  - Admin → ตัววิ่งข่าว tab → เห็นแค่ Settings panel (ไม่มี Manual list แล้ว)
  - NewsForm Switch → ติ๊กแล้วเห็นข้อความเขียว + คลิกซ้ำเพื่อปิดได้

### Nice to have
- [ ] **Drag-and-drop reorder ใน ticker** — ตอนนี้ append ท้ายเสมอ ถ้าอยาก reorder ต้องแก้ `ticker_order` ใน DB
- [ ] **ลบตาราง `ticker_items` ออกจาก DB** — ตาราง legacy ไม่มีโค้ดใช้แล้ว (ปลอดภัยต่อ migration ใหม่ `032_drop_ticker_items.sql`)
- [ ] **Migration prefix cleanup** — มี duplicates 026/027 (026_homepage_testimonials_partners + 026_school_dashboard_table_data, 027_ticker_items + 027_school_dashboard_staff_columns) ปัญหาเก่า ควร rename สักวัน
- [ ] **`/news/:id` path segment route** — ถ้าอยาก SEO friendly URL (ตอนนี้ใช้ query string `?id=`) ต้องเพิ่ม route ใน App.tsx + แก้ News.tsx รับทั้ง 2 รูปแบบ

### Watching
- หลัง user ใช้งานจริง — ถ้า admin บ่นว่าติ๊ก/เลิกติ๊ก ticker ลำบาก ค่อย refactor เป็น dedicated TickerPicker UI ในแทป "ตัววิ่งข่าว"

---

## Files touched today

| File | Change |
|---|---|
| `supabase/migrations/031_news_show_in_ticker.sql` | NEW |
| `src/integrations/supabase/types.ts` | manual edit (regen ไม่ได้ — MCP no permission) |
| `src/hooks/useTickerItems.ts` | filter show_in_ticker, link `/news?id=`, ลบ manual source |
| `src/components/home/NewsTicker.tsx` | full refactor (duplicate items pattern) |
| `src/components/home/HomeMainContent.tsx` | fix query, ลบ DUMMY_NEWS, fix all 5 Links |
| `src/components/admin/news/NewsForm.tsx` | + ticker toggle + counter + UX hint |
| `src/components/admin/news/NewsList.tsx` | + ticker badge |
| `src/components/admin/news/NewsManagement.tsx` | + show_in_ticker, ticker_order ใน NewsItem interface |
| `src/components/admin/news/TickerManagement.tsx` | trim เหลือแค่ Settings |
| `src/services/news.service.ts` | fix is_published typo |
| `src/index.css` | + @keyframes news-ticker-rtl |

## Commits
1. `355f474` — base news fix + migration 031
2. `f0fabe3` — link/Manual cleanup/UX hint
3. `79ddd42` — mobile ticker refactor
