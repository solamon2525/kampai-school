# งานค้างรอทำต่อ — Import เกม HTML เก่า → educational-hub

> สถานะ: deploy production แล้ว (commit `f53cfa6`, deploy READY ที่ kampai-school.vercel.app)
> เจ้าของเกม: **นายณัฐพงศ์ สิงห์ชมภู** (staff_id `a5f53911-b9cb-465a-b963-7202bcb907b1`)
> วันที่สรุป: 2026-05-19

## บริบทย่อ

Import เกม HTML standalone 17 ไฟล์ จาก archive เก่า (`D:\เก่า\kampai-school-app\games\`) เข้าระบบ educational-hub โดย:

- Copy ไฟล์ทั้งหมดไป `public/games/{math,tech,thai}/`
- Migration `062_seed_natthapong_games.sql` ผูก items 17 รายการ (item_type=`link`, category=`games`)
- แยกย่อยด้วย field `subject` = คณิตศาสตร์ / เทคโนโลยี / ภาษาไทย
- ปุ่ม "กลับ" ในเกมยัง link ไป `/games/index.html` (catalog เดิม) — เก็บตามต้นฉบับ

## เสร็จแล้ว ✅

- [x] Copy 17 ไฟล์เกม + catalog (index/leaderboard/scores) + img/ → `public/games/`
- [x] Migration 062 applied + idempotent (NOT EXISTS guard บน external_url)
- [x] Seed 17 items: math 3 / tech 2 / thai 12
- [x] Build verify (pnpm build, 13s, ไม่มี error)
- [x] Rebase + push main → Vercel deploy state = READY

## ค้างรอทำต่อ 🔜

### 1. Verify production จริง

ตรวจ 3 จุดบน live URL:

- [ ] เปิด `https://kampai-school.vercel.app/educational-hub/a5f53911-b9cb-465a-b963-7202bcb907b1` → ต้องเห็น **17 รายการ** ในหมวด "คลังเกมการศึกษา"
- [ ] คลิก "**ตกปลามาตรา ตัวสะกด 2**" → ต้องเปิด tab ใหม่เล่นได้ (URL-encoded filename)
- [ ] เปิดเกมแล้วเช็ค `<title>` ใน browser ว่ามี **mojibake** หรือไม่ (PowerShell อ่านเพี้ยน แต่ browser อาจ render ปกติ — ถ้าเพี้ยนต้องแก้ encoding ของ HTML)

### 2. Dedupe ไฟล์ซ้ำ

ไฟล์ที่ขนาดเท่ากัน/ชื่อใกล้กัน ตอนนี้ publish ทั้งหมด — เข้า `/admin → educational-hub` แล้ว **unpublish** ตัวที่ไม่ต้องการ:

- [ ] `waipot.html` (39,541 B) ↔ `wipod.html` (51,425 B) — "ไวพจน์ v1/v2"
- [ ] `attack-noun.html` (12,484 B) ↔ `attack-on-noun.html` (68,559 B) ↔ `attnoun.html` (54,043 B) — "Attack on Noun v1/v2/v3"
- [ ] `ppp.html` (42,279 B) ↔ `ตกปลามาตรา ตัวสะกด2.html` (42,279 B) — **ขนาดเท่ากันเป๊ะ** (น่าจะไฟล์เดียวกัน)

### 3. ปรับแต่ง content

- [ ] **Title** ให้สื่อความหมายชัดเจน (ตอนนี้ตั้งตามชื่อไฟล์คร่าว ๆ เช่น "PPP", "สอนตัวเลข")
- [ ] **Description** ของแต่ละเกม (ตอนนี้ไม่มี — empty)
- [ ] **Thumbnail** ของแต่ละเกม (ตอนนี้แสดง default `ExternalLink` icon) — อัปโหลดผ่าน admin form ฟิลด์ `thumbnail_url`
- [ ] **Tags / grade_levels** (ตอนนี้ว่าง — array `{}`)

### 4. ปุ่ม "กลับ" ในเกม

ปัจจุบันทุก HTML link ไป `../index.html` → ลงเอยที่ `/games/index.html` (catalog เก่า)

ถ้าต้องการให้กลับเข้าระบบ:

- [ ] Patch HTML 17 ไฟล์ → เปลี่ยน href เป็น `/educational-hub/a5f53911-b9cb-465a-b963-7202bcb907b1`
- [ ] หรือลบ catalog (`/games/index.html`, `leaderboard.html`, `scores.html`) แล้วใช้ตัว educational-hub แทน

### 5. งานเสริมที่อาจคิดต่อ

- [ ] **Game Catalog widget** ใน homepage (ตอนนี้ user ต้องเข้า /educational-hub แล้วคลิกครูณัฐพงศ์)
- [ ] **OG image / sitemap entry** ของ educational-hub (ตามงานค้าง SEO ที่บันทึกใน memory)
- [ ] **ครูคนอื่น** import เกม/สื่อตัวเอง ผ่าน `/teacher/edu-hub` (Teacher Portal มีอยู่แล้ว)

## ไฟล์ที่เกี่ยวข้อง

| Path | บทบาท |
|---|---|
| `public/games/{math,tech,thai}/*.html` | ไฟล์เกม static (serve เป็น 1-1 จาก Vite) |
| `public/games/index.html` | Catalog เก่า (ปลายทางปุ่มกลับ) |
| `supabase/migrations/062_seed_natthapong_games.sql` | Seed items (idempotent) |
| `src/components/educational-hub/EduHubItemCard.tsx` | Component ที่ render card + handle click `link` type |
| `src/services/educational-hub.service.ts` | Query layer (categories, items, RPC counters) |
| `src/pages/EducationalHubTeacher.tsx` | หน้าครูแต่ละท่าน — รวม section ตาม category |

## คำสั่งที่อาจได้ใช้

```bash
# ตรวจจำนวน items ของครูณัฐพงศ์
# SELECT COUNT(*) FROM educational_hub_items
# WHERE owner_staff_id = 'a5f53911-b9cb-465a-b963-7202bcb907b1';
# → ต้องได้ 17

# Re-apply migration (idempotent)
# supabase db push   # หรือ apply ผ่าน MCP execute_sql
```

## หมายเหตุ

- Hub UI ไม่ render ในเครื่อง preview ถ้าไม่มี `.env` (VITE_SUPABASE_URL/KEY) — environment issue ไม่ใช่ปัญหา code
- ไฟล์ Thai filename ใช้ URL-encoded ใน `external_url` แล้ว — ทุก URL fetch test pass 200
