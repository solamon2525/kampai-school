/* config.js — พารามิเตอร์เกม (แก้ค่าตรงนี้ที่เดียว ไม่ต้องแตะ game.js)
   ⚠️ เปลี่ยน SLUG เป็น slug จริง (ตรงกับ educational_hub_items.game_slug) ก่อนใช้งาน */
window.GAME_CONFIG = {
  SLUG: 'placeholder-slug',   // ⚠️ TODO: ใส่ slug จริง
  BGM: 'cheerful',            // เพลงพื้นหลัง: cheerful/calm/warm/playful/bright/mellow

  // ── การเล่น ──
  SPEED: 9,                   // ความเร็วเลื่อนตะกร้าด้วยปุ่ม (px/เฟรม)
  LIVES: 3,                   // จำนวนชีวิต (โหมดผจญภัย)
  TIME_SECONDS: 60,           // เวลาโหมดแข่งเวลา (วินาที)

  // ── คะแนน + คอมโบ ──
  GOOD_POINTS: 10,            // คะแนนพื้นฐานต่อของดี (⭐)
  BONUS_POINTS: 50,           // คะแนนของโบนัส (💎)
  COMBO_STEP: 3,              // รับติดกันครบกี่ครั้ง คูณคะแนน +1 เท่า (x2 ที่ครั้งที่ 3, x3 ที่ 6 ...)
  COMBO_MAX: 5,               // ตัวคูณคอมโบสูงสุด

  // ── การเกิดของ + ไต่ระดับความยาก ──
  SPAWN_START_MS: 850,        // ระยะปล่อยของตอนเริ่ม (ms)
  SPAWN_MIN_MS: 380,          // ปล่อยถี่สุด (ms) เมื่อเลเวลสูง
  FALL_START: 2.4,            // ความเร็วตกตอนเริ่ม
  LEVEL_EVERY_MS: 12000,      // ไต่ 1 เลเวลทุกกี่ ms
  SPEED_RAMP: 0.5,            // ความเร็วตกเพิ่มต่อเลเวล
  SPAWN_RAMP_MS: 70,          // ระยะปล่อยลดลงต่อเลเวล (ถี่ขึ้น)

  // ── โอกาสเกิดของแต่ละชนิด (ที่เหลือ = ของดี) ──
  BAD_CHANCE: 0.22,           // 💣 ของร้าย (โดน = เสียชีวิต)
  BONUS_CHANCE: 0.07,         // 💎 โบนัส (หายาก คะแนนมาก)

  STAR_THRESHOLDS: [60, 180, 360],  // คะแนนขั้นต่ำสำหรับ 1 / 2 / 3 ดาว (จอจบ)

  // ── ออนไลน์ ──
  ENABLE_ONLINE: true,        // true = เปิดปุ่ม "🌐 ออนไลน์" (แข่งสดต่างเครื่อง · race ตามเวลา)
  ONLINE_DURATION: 60,        // เวลาแข่งออนไลน์ (วินาที)
};
