/* config.js — พารามิเตอร์ของเกม Laser Reflect */
window.GAME_CONFIG = {
  SLUG: 'laser-reflect',      // slug ตรงกับฐานข้อมูล
  BGM: 'calm',                 // เพลงพื้นหลัง: calm (เข้ากับการคิดคำนวณวิถี)

  // ── การเล่น ──
  LIVES: 3,                    // จำนวนชีวิตเริ่มต้นในโหมดผจญภัย
  TIME_SECONDS: 60,            // เวลาโหมดแข่งเวลา (วินาที)
  GRID_SIZE: 10,               // ขนาดตารางพิกัดมาตรฐาน 10x10 (0 ถึง 10)

  // ── คะแนน ──
  POINTS_PER_LEVEL: 100,       // ผ่าน 1 ด่านได้ 100 คะแนน
  BONUS_TIME_MULTIPLIER: 2,    // โบนัสเวลาที่เหลือ (วิละ 2 คะแนน)
  
  // ── เกณฑ์ดาว ──
  STAR_THRESHOLDS: [200, 500, 800], // คะแนนสำหรับ 1 / 2 / 3 ดาว (จอสรุปผล)
};
