/* config.js — พารามิเตอร์การตั้งค่าเกมคณิตศาสตร์ RPG การคูณ ป.4 (window.GAME_CONFIG)
   กำหนดค่าตัวแปรกลางเพื่อให้ปรับจูนความยากง่ายและการเล่นได้จากที่นี่ที่เดียว */
window.GAME_CONFIG = {
    SLUG: 'multiplication-rpg',         // ตัวเชื่อมโยงกับฐานข้อมูล (slug ต้องตรงกัน)
    BGM: 'playful',                     // เพลงพื้นหลังเริ่มต้น: cheerful, playful, bright, mellow

    // ── ระบบการเล่น ──
    LIVES: 3,                           // พลังชีวิตเริ่มต้น (จำนวนหัวใจ ❤️)
    TIME_LIMIT_SEC: 20,                 // เวลาต่อหนึ่งคำถาม (วินาที)
    SPEED_CHALLENGE_TIME: 10,           // เวลาสำหรับด่านความเร็วสูง (วินาที)
    COMBO_STEP: 3,                      // จำนวนคอมโบที่ทำให้คริติคอลแดเมจเพิ่มขึ้น
    CRITICAL_CHANCE: 0.25,              // โอกาสสุ่มโจมตีคริติคอลติด (25%)
    COMBO_MAX: 5,                       // ตัวคูณคอมโบสูงสุด

    // ── โครงสร้างด่าน ──
    STAGES_PER_CHAPTER: 10,             // จำนวนด่านต่อบทเรียน
    TOTAL_CHAPTERS: 3,                  // จำนวนบททั้งหมด
    BOSS_STAGES: [10, 20, 30],          // ด่านที่เป็นบอสประจำบทเรียน

    // ── คะแนนและโบนัส ──
    POINTS_CORRECT: 100,                // คะแนนพื้นฐานตอบถูก
    POINTS_WRONG: -20,                  // หักคะแนนตอบผิด
    POINTS_SPEED_BONUS: 50,             // คะแนนพิเศษเมื่อตอบเสร็จเร็วภายในครึ่งหนึ่งของเวลา
    MEDAL_GOLD_SCORE: 2500,             // คะแนนขั้นต่ำสำหรับเหรียญทอง
    MEDAL_SILVER_SCORE: 1500,           // คะแนนขั้นต่ำสำหรับเหรียญเงิน
    MEDAL_BRONZE_SCORE: 800,            // คะแนนขั้นต่ำสำหรับเหรียญทองแดง

    // ── การตั้งค่าดาว (Star Thresholds) ──
    STAR_THRESHOLDS: [800, 1500, 2500]  // คะแนนด่านรับ 1 / 2 / 3 ดวงตามลำดับ
};
