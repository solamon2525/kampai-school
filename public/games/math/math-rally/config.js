/* config.js — พารามิเตอร์เกม "รถซิ่งสูตรคูณ" (แก้ค่าตรงนี้ที่เดียว ไม่ต้องแตะ game.js) */
window.GAME_CONFIG = {
  SLUG: 'math-rally',
  BGM: 'playful',

  // ── สนามแข่ง ──
  TRACK_LEN: 1000,            // ระยะถึงเส้นชัย (หน่วย) ≈ ตอบถูก ~12 ข้อ + ไอเทม
  RACE_CAP_S: 180,            // เพดานเวลาแข่ง vs คอม (วินาที) — หมดแล้วเทียบระยะ

  // ── ความเร็วรถ (หัวใจ: ตอบถูกไว ตอบผิดช้า) ──
  BASE_SPEED: 8,              // ความเร็วเริ่ม (หน่วย/วิ)
  MAX_SPEED: 30,              // เพดานความเร็ว
  SPEED_GAIN: 6,              // ตอบถูก speed เพิ่ม
  SPEED_DECAY: 1.2,           // speed ลดต่อวินาที (ต้องตอบต่อเนื่อง)
  BOOST_DIST: 30,             // ตอบถูกพุ่งทันที (หน่วย) × ตัวคูณคอมโบ
  FAST_ANSWER_MS: 2000,       // ตอบเร็วกว่านี้ = "สายฟ้า ⚡"
  FAST_BONUS_DIST: 15,        // โบนัสระยะสายฟ้า
  WRONG_SPEED_MULT: 0.5,      // ตอบผิด speed คูณ
  WRONG_LOCK_MS: 1200,        // ตอบผิดล็อกปุ่ม (รถควัน)
  TIMEOUT_SPEED_MULT: 0.7,    // หมดเวลา/ข้อ = ผิดอ่อน
  Q_TIME_MS: 6000,            // เวลาต่อข้อ

  // ── คะแนน + คอมโบ ──
  CORRECT_POINTS: 10,         // คะแนนต่อข้อถูก (× คอมโบ × ดาว)
  COMBO_STEP: 3,              // ทุก N streak ตัวคูณ +0.5
  COMBO_MAX_MULT: 2.5,
  WIN_BONUS: 200,             // ถึงเส้นชัยก่อน
  LIGHTNING_POINTS: 5,        // โบนัสคะแนนต่อสายฟ้า

  // ── ไอเทม (กล่อง ? บนถนน — วิ่งชนเก็บ ใช้ทันที) ──
  ITEM_EVERY: 120,            // ระยะห่างกล่องไอเทม (หน่วย)
  NITRO_DIST: 80,             // 🚀 พุ่งทันที
  STAR_MS: 10000,             // ⭐ คะแนน ×2 นาน (ms)
  TURTLE_MS: 3000,            // 🐢 คู่แข่งช้าลง (vs คอมเท่านั้น)

  // ── AI คู่แข่ง (vs คอม) ──
  AI: {
    easy: { min: 4000, max: 6000, acc: 0.70 },   // ตอบทุก 4-6s ถูก 70%
    mid:  { min: 3000, max: 5000, acc: 0.85 },
    hard: { min: 2200, max: 4000, acc: 0.95 },
  },

  // ── ออนไลน์ ──
  ENABLE_ONLINE: true,
  ONLINE_DURATION: 150,       // เพดานเวลาออนไลน์ (วิ) — ปกติจบก่อนเพราะมีคนถึงเส้นชัย
};
