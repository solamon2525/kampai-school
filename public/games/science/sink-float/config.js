/* config.js — พารามิเตอร์เกม "จม หรือ ลอย?" (sink-float)
   แก้ค่าตรงนี้ที่เดียว ไม่ต้องแตะ game.js */
window.GAME_CONFIG = {
  SLUG: 'sink-float',          // ตรงกับ educational_hub_items.game_slug
  BGM: 'playful',              // เพลงพื้นหลัง

  // ── การเล่น ──
  LIVES: 3,                    // ชีวิต (โหมดแข่งเร็ว)
  TIME_SECONDS: 60,            // เวลาโหมดแข่งเร็ว (วินาที)
  REVEAL_MS: 1150,             // เวลาโชว์เฉลย/แอนิเมชันก่อนของชิ้นถัดไป (ms)

  // ── คะแนน + คอมโบ ──
  GOOD_POINTS: 10,             // คะแนนต่อข้อที่ตอบถูก
  TIME_BONUS: 5,               // โบนัสตอบถูกเร็ว (โหมดแข่งเร็ว — ตอบใน 2 วิแรก)
  COMBO_STEP: 3,               // ตอบถูกติดกันครบกี่ครั้ง คูณคะแนน +1
  COMBO_MAX: 5,                // ตัวคูณคอมโบสูงสุด

  // ── ออนไลน์ ──
  ENABLE_ONLINE: true,         // เปิดปุ่ม 🌐 ออนไลน์ (แข่งสดต่างเครื่อง)
  ONLINE_DURATION: 60,         // เวลาแข่งออนไลน์ (วินาที)
};
