# Kampai School Printable Worksheet Development Guidelines (คู่มือการสร้างใบงานพิมพ์การศึกษา)

คู่มือนี้กำหนดมาตรฐานและการใช้งานแม่แบบสำหรับสร้าง **ใบงานพิมพ์อัจฉริยะ (Printable Worksheet System)** ของโรงเรียนบ้านคำไผ่

---

## 1. ไฟล์แม่แบบหลัก (Worksheet Boilerplate Template)
- **แม่แบบเริ่มต้น:** `public/games/_template-worksheet.html`
- **ตัวอย่างใบงานที่ใช้วิธีนี้สำเร็จ:** `public/games/math/multiplication-worksheet.html`

เมื่อต้องการสร้างใบงานใหม่สำหรับวิชาอื่นๆ (เช่น การบวก-ลบ, การหาร, ภาษาไทย, ภาษาอังกฤษ, วิทยาศาสตร์):
```bash
cp public/games/_template-worksheet.html public/games/{subject}/{slug}-worksheet.html
```

---

## 2. องค์ประกอบสำคัญ 7 ประการในแม่แบบใบงาน (7 Core Features)

1. **ตราโรงเรียนบ้านคำไผ่จริง (Official Kampai School Logo):**
   - URL: `https://lkpqssbqxxpasidfqhpb.supabase.co/storage/v1/object/public/school-images/logo/1778157862905_c2swwm.webp`
   - แสดงมินิโลโก้ในส่วนหัวกระดาษ (`.sheet-head .school-name`) และแสดงตราขนาดใหญ่ในส่วนหน้าปกเล่ม (`.cover-sheet`)

2. **ระบบดึงข้อมูลครูผู้สอนอัตโนมัติ (Dynamic Supabase Teacher Selector):**
   - ทุกใบงานเรียก `window.KampaiWorksheet.loadTeachers()` จาก `public/games/worksheet-runtime.js` เท่านั้น ห้ามประกาศ URL/key หรือเขียน `fetch('/rest/v1/...')` ซ้ำในไฟล์ใบงาน
   - runtime ดึงเฉพาะข้อมูลอ่านได้แบบ public (`name,position,staff_type,photo_url`) ด้วย publishable key ภายใต้ RLS และห้าม mutation ทุกชนิด
   - ย้ายชื่อครูผู้สอนไปแสดงที่ด้านล่างกระดาษในแถบส่งผู้ปกครอง (`.parent-slip`) และแถบท้ายกระดาษ (`.sheet-foot`) เพื่อไม่ให้ทับซ้อนกับ QR Code บนส่วนหัว

3. **ส่วนหัวนักเรียนที่สะอาด อ่านง่าย (Clean 1-Line Profile Header):**
   - แถวเดียวไม่ตัดบรรทัด: `ชื่อ–นามสกุล` (เส้นยาว) + `เลขที่` (สั้น) — หรือแบบมี `ชั้น` กลางด้วย (เช่น rect-area)
   - Layout กลางอยู่ที่ `worksheet-modes.css` (`.sheet .student`) + `worksheet-topic.css` / `worksheet-topic.js` สำหรับใบงาน topic — ห้ามคัดลอก flex/grid แยกในไฟล์รายวิชาจนทำให้เลขที่ตกบรรทัด
   - โครง markup: `.student > .field|.student-name|.student-no` มี `<span class="lbl">` + `<span class="blank">` คู่กันเสมอ

4. **การจัดวางพื้นที่แบบ Edge-to-Edge & Isolated QR Code Header:**
   - ส่วนหัวกระดาษ `.sheet-head` ถูกล็อกด้วย `padding-right: 28mm` เพื่อกันพื้นที่ฝั่งขวาบนสำหรับ QR Code นำทางไปยังสื่อดิจิทัลโดยเฉพาะ
   - ขอบกระดาษ `.sheet` ตั้งค่า `padding: 5mm 6mm` เพื่อเปิดพื้นที่เนื้อหาเต็มแผ่น A4

5. **ระบบโหมดโจทย์ใหญ่ ตัวเต็มช่อง (Flexible 5 & 10 Question Grid Modes):**
   - **โหมด 10 ข้อ/หน้า (`.count-10`):** ตาราง 2 คอลัมน์ × 5 แถว, ช่องกริดขนาดใหญ่ `33px × 31px`, ตัวเลขโจทย์ขยายใหญ่เบิ้ม `17.5pt` (ตัวหนา 800) ตัวใหญ่เต็มช่องสะใจ
   - **โหมด 5 ข้อ/หน้า (`.count-5`):** ตาราง 1 คอลัมน์ × 5 แถว, ช่องกริดใหญ่พิเศษ `37px × 37px`, ตัวเลขโจทย์ `16.5pt`
   - **ระยะบรรทัดคำตอบ (`.ans`):** เว้นระยะห่าง `margin-top: 3.5mm` ลงมาจากตารางโจทย์เพื่อให้นักเรียนเขียนคำตอบได้ง่าย สบายตา

6. **ระบบสร้างหลายหน้าพร้อมกัน (Multi-Page Batch Generator):**
   - รองรับการสร้าง 1, 2, 3, 5, 10 หน้าในคลิกเดียว พร้อมระบบสุ่มโจทย์แยกทุกหน้า, ไต่ระดับความยาก ป.4 ➔ ป.5 ➔ ป.6 และการสร้างรหัสชุดนักเรียน (Batch A, B, C...)

7. **ระบบรวมเล่มแบบฝึกหัด (Booklet System) & แถบผู้ปกครอง (Parent Slip):**
   - มีโหมดหน้าปกรวมเล่มพร้อมตารางสะสมดาวความก้าวหน้า (Skill Tree Stamp Sheet)
   - แถบรอยปรักด้านล่างสุดสำหรับส่งแจ้งผลการทำแบบฝึกหัดให้ผู้ปกครองรับทราบ (`✂️ แถบส่งผู้ปกครอง`)

8. **โหมดตามวัตถุประสงค์การสอน (Teaching Purpose Modes):**
   - ใช้ไฟล์กลาง `public/games/worksheet-modes.js` และ `public/games/worksheet-modes.css` เพื่อให้ใบงานทุกวิชาใช้กติกาเดียวกัน
   - **ใบงานปกติ:** คงรูปแบบเดิม รวมถึงมาตรฐาน บันไดระดับ และรวมเล่มของแต่ละวิชา
   - **แยกระดับ A–B–C:** สร้าง 3 ชุดพร้อมกัน — ชุด A ทบทวน 5 ข้อพร้อมตัวช่วย, ชุด B มาตรฐาน 10 ข้อ และชุด C ท้าทาย 5 ข้อพร้อมคำสั่งให้อธิบายวิธีคิด
   - **ตรวจเร็วท้ายคาบ:** 5 ข้อในหน้าเดียว พร้อมเวลาแนะนำ 5 นาทีและช่องสะท้อนการเรียนรู้
   - **วินิจฉัยก่อนเรียน:** 5 ข้อ พร้อมตารางให้ครูบันทึกผ่าน/ซ่อมรายข้อและระบุทักษะที่ควรเสริม
   - **ซ่อมเสริมเฉพาะจุด:** 5 ข้อ พร้อมตัวช่วยทีละขั้นและพื้นที่ทำงานที่อ่านง่าย
   - โหมดพิเศษจะล็อกจำนวนหน้าและปิดรูปแบบรวมเล่มชั่วคราว เมื่อกลับสู่โหมดปกติระบบจะคืนค่าที่ครูเลือกไว้ก่อนหน้า
   - ตารางวินิจฉัยและตัวช่วยซ่อมเสริมใช้ข้อความเฉพาะวิชา ได้แก่ การคูณ การหาร พื้นที่รูปเรขาคณิต ภาษาไทย ภาษาอังกฤษ วิทยาศาสตร์ และวิทยาการคำนวณ/สังคมศึกษา
   - `public/games/math/rect-area-worksheet.html` รองรับการเลือกหัวข้อรูปทรง จำนวนหน้า และโหมดกลางแล้ว โดยโหมด A–B–C จะใช้รูปทรงเดียวกันทั้งสามระดับ
   - ใบงานการหารใช้ 5 ข้อต่อหน้าเท่านั้น และใช้กระดานตั้งหารยาวจริง: ตัวหารอยู่ซ้าย ตัวตั้งอยู่ใต้เส้น ผลหาร ผลคูณ ผลลบ และเลขที่ดึงลงต้องตรงคอลัมน์ค่าประจำหลักเดียวกัน ห้ามแยกเป็นป้าย “ขั้น 1/ขั้น 2” หรือสมการข้อความข้างกระดาน

---

## 3. Worksheet Contract (บังคับทุกไฟล์)

ไฟล์ `*-worksheet.html` และแม่แบบต้องมี contract ต่อไปนี้ เพื่อให้ runtime/โหมดกลางทำงานโดยไม่ต้องเขียน adapter รายวิชา:

- `.toolbar-ctrls` ครอบ control ทั้งหมด
- `#selStyle` และ `#selPageCount` (ซ่อนได้แต่ห้ามลบ)
- `#selCount` เมื่อใบงานรองรับ 5/10 ข้อ
- `#pages > .sheet`, ภายในมี `.questions > .q`
- ประกาศ `function render()` ที่เรียกซ้ำได้โดยไม่สะสม DOM/event เก่า
- โหลด `worksheet-runtime.js`, `worksheet-modes.css` และ `worksheet-modes.js` พร้อม query version เดียวกัน
- ใบงานที่จับคู่สื่อเฉพาะเรื่องต้องมี `<meta name="worksheet-source-media">` และ `<meta name="curriculum-indicators">`; ค่า source ต้องตรงกับ `sourceMediaUrl` ใน config และชี้ไปยังไฟล์ที่มีอยู่จริง
- ใบงานเฉพาะเรื่องใช้ `worksheet-topic.css` + `worksheet-topic.js` เป็น shell กลางสำหรับ A4, QR สื่อ, ส่วนหัวนักเรียน, เฉลย และ footer ห้ามคัดลอก shell ไปไว้รายไฟล์
- control ที่ครูต้องเลือกต้องมี `<label>` หรือ `aria-label` ชัดเจน
- ห้ามสร้าง mode logic, teacher fetch หรือ print CSS ซ้ำในไฟล์รายวิชา ถ้ามีพฤติกรรมร่วมให้แก้ที่ runtime/modes กลาง

## 3.1 บันทึกชุดใบงาน (Worksheet Sets Cloud)

ปัญหา: ใบงานสุ่มด้วย RNG ทุกครั้งที่เปิด จึงไม่มีชุดเดิมให้กลับมาเฉลยบนจอหลังให้นักเรียนไปแล้ว

แนวทาง:

| ส่วน | ไฟล์ / ตาราง |
|---|---|
| DB | `worksheet_sets` (migration 419) — `seed` + `config` jsonb + `access` (`private`\|`link`) |
| Engine | `public/games/worksheet-sets.js` — `KampaiWorksheetSets` (mulberry32, save/load/list, mountToolbar) |
| Runtime | `public/games/worksheet-runtime.js` → `loadSetsModule()` (ห้ามฝัง URL/key ซ้ำในไฟล์ใบงาน) |
| Topic shell | `public/games/worksheet-topic.js` ผูก seed + toolbar ชุดอัตโนมัติ |
| React | `src/services/worksheet-sets.service.ts` + `WorksheetSetsPanel` ในพอร์ทัลครู/แอดมิน |

Workflow ครู:

1. สร้าง/สุ่มชุด → กด **บันทึกชุด** (ต้องล็อกอินพอร์ทัลครูบนโดเมนเดียวกัน — ใช้ session ใน `localStorage`)
2. พิมพ์ให้นักเรียน / **คัดลอกลิงก์** (`?set=<uuid>`)
3. วันหลังเปิดลิงก์เดิมบนจอ → โจทย์ตรงเดิม → ใช้เฉลยทีละข้อ (เช่น rect-area)

กฎ:

- ชื่อชุดตั้งอัตโนมัติทุกใบงานที่ `mountToolbar` (รูปแบบ `{ชื่อใบงาน} - {หัวข้อ} · {N} หน้า · ชุด {k}`) — ช่องชื่อเป็น readonly ไม่ต้องพิมพ์; ปรับด้วย `titlePrefix` / `topicLabels` / `suggestTitle` ได้
- สุ่มใหม่ = seed ใหม่ (ไม่ทับชุดที่บันทึกจนกดบันทึก)
- ยังไม่ล็อกอิน: โหลดชุด `access=link` ได้ แต่ปุ่มบันทึกจะบอกให้เข้าพอร์ทัลก่อน
- verify:worksheet ตรวจ contract ชุดสำหรับ rect-area และใบงานที่ใช้ topic/runtime กลาง + auto title ใน engine กลาง

## 4. Process Scaffold Rule (โจทย์ต้องมีพื้นที่ทำวิธี)

ถ้าตัวชี้วัดประเมิน “กระบวนการ” ห้ามแสดงเพียงโจทย์และสั่งกว้าง ๆ ว่า *แสดงวิธีทำ* ต้องเตรียม scaffold ที่ตรงกับทักษะ:

- หารยาว → ใช้ 5 ข้อต่อหน้าเพื่อให้มีพื้นที่เขียนจริง ตัวหารอยู่ซ้าย ตัวตั้งใต้เส้น ช่องผลหารอยู่ด้านบน; แถวผลคูณต้องวางใต้จำนวนที่นำมาหาร มีเส้นลบ และวางเศษกับเลขที่ดึงลงตรงหลักถัดไป เมื่อเปิดเฉลยต้องเติมตัวเลขลงตำแหน่งเดิมโดยไม่เปลี่ยน layout ห้ามใช้ป้าย “ขั้น 1/ขั้น 2”, สมการข้อความ หรือป้าย `เฉลย ...` ท้ายข้อ
- คูณแนวตั้ง → ช่องตรงหลัก ช่องทด และผลคูณย่อย
- พื้นที่ → ภาพกำกับ สูตร ช่องแทนค่า วิธีคำนวณ และหน่วย²
- ภาษา → ช่องจำแนกคำ/ส่วนประกอบประโยคตามสิ่งที่วัด
- วิทยาศาสตร์/เทคโนโลยี → ตารางสังเกต ลำดับขั้น หรือช่องเหตุผลตามภารกิจ

พื้นที่เขียนต้องรองรับลายมือนักเรียนจริง ไม่ใช้เส้นสั้นเพื่อให้หน้าดูโล่ง และห้ามให้เฉลยเปลี่ยน layout เมื่อเปิด/ปิด

## 5. Mode และ Diagnostic Rules

- A–B–C ต้องเป็น 5/10/5 ข้อเสมอ ยกเว้นใบงานที่ประกาศ `data-fixed-count` ตามข้อจำกัดด้านพื้นที่ของทักษะ (เช่น หารยาว = 5/5/5) ระบบต้องล็อกจำนวนหน้า/จำนวนข้อและคืนค่าเดิมเมื่อออกจากโหมด
- วินิจฉัยและซ่อมเสริมต้องใช้ทักษะเฉพาะวิชา ห้ามใช้ข้อความกลางเพียงชุดเดียว
- โหมด 10 ข้อเน้นความกระชับ แต่ต้องเหลือขั้นตอนสำคัญครบ; โหมด 5 ข้อต้องขยายช่องเขียน ไม่ใช่เพียงลบโจทย์ครึ่งหนึ่ง
- สุ่มหลายหน้าต้องได้โจทย์ต่างกัน แต่ A–B–C ต้องรักษาหัวข้อ/ทักษะเดียวกันเพื่อเปรียบเทียบระดับได้

## 6. Print, Person และ Styling Exceptions

- ใบงานเป็น **print-only document**: ชื่อครูใน native selector, footer และช่องลงชื่อแสดงชื่อเต็ม+ตำแหน่งได้โดยไม่ใช้ `PersonAvatar`; ข้อยกเว้นนี้ไม่ครอบคลุม React UI, card, table หรือ custom dropdown
- standalone worksheet ใช้ CSS ปกติแทน Tailwind ได้ แต่สีใหม่ต้องประกาศเป็น CSS custom property ใน `:root`/shared stylesheet แล้วเรียกผ่าน `var(...)`; ห้ามกระจาย hex ใหม่ใน selector รายจุด
- QR/รูป/ข้อความทุกส่วนต้องพิมพ์ขาวดำแล้วยังอ่านและแยกขอบเขตได้ ห้ามพึ่งสีอย่างเดียว

## 7. Verification Gate (ก่อน commit ทุกครั้ง)

1. รัน `pnpm verify:worksheet [path]` หรือไม่ใส่ pathเพื่อตรวจทั้งหมด
2. ทดสอบผ่าน HTTP server/Vite เท่านั้น — **ห้ามใช้ `file://` เป็นผลทดสอบสุดท้าย** เพราะ root assets `/games/...` จะไม่โหลด
3. เปิด browser จริงและทดสอบอย่างน้อย: ปกติ 10 ข้อ, ปกติ 5 ข้อ, A–B–C, วินิจฉัย, ซ่อมเสริม และระดับที่เนื้อหายาวที่สุด
4. ทุก `.sheet` ต้องพอดี A4 210×297 มม.; ห้ามอาศัย `overflow:hidden` ซ่อนส่วนที่เกิน
5. ตรวจเฉลยครู, สุ่มใหม่, รายชื่อครู และ Print Preview/PDF อย่างน้อย 1 หน้า
6. ตัวตรวจต้องยืนยันว่าไฟล์ใบงานทุกไฟล์ (ยกเว้น template) มี URL อยู่ใน migration ของคลังใบงาน เพื่อป้องกันกรณีมีไฟล์แต่ production ไม่แสดง
7. หลัง apply migration ต้องรัน `pnpm verify:worksheet:production` ให้จำนวนและ URL ของไฟล์ `*-worksheet.html` ตรงกับรายการ published ในหมวด `worksheets` ทุกครั้ง ห้ามสรุปว่างานขึ้นเว็บแล้วจากการ push หรือ static verification เพียงอย่างเดียว

คำสั่งแนะนำ:

```bash
pnpm verify:worksheet
pnpm verify:worksheet public/games/math/division-worksheet.html
pnpm verify:worksheet:production
```

### Automatic Publish Gate (ใบงาน)

หลังแก้หรือสร้างใบงานและผ่าน Verification Gate แล้ว ให้ commit/push อัตโนมัติตามกฎ `Automatic Commit & Push` ใน `AGENTS.md` โดย stage เฉพาะใบงาน, shared runtime/style, test, migration และเอกสารที่เกี่ยวข้องกับงานนั้นเท่านั้น

- ถ้าแก้เฉพาะ HTML/CSS/JS ของใบงาน: ต้องผ่าน `verify:worksheet` รายไฟล์และตรวจผ่าน HTTP/browser จริงก่อนเผยแพร่
- ถ้าเพิ่มหรือแก้รายการคลังใบงานใน migration: ต้อง apply migration และผ่าน `verify:worksheet:production` ก่อนสรุปว่าแสดงบนเว็บแล้ว
- ถ้าตรวจ browser, A4 หรือ production parity ไม่ครบตามสิ่งที่เปลี่ยน: ห้าม auto push และต้องรายงานจุดที่ยังตรวจไม่ได้

## 8. Paired Media Batch Rule

สร้างใบงานครั้งละ 5 เรื่อง โดยเลือกจากสื่อที่เผยแพร่และมี indicator mapping แล้ว กระจายรายวิชาและไม่ซ้ำใบงานเดิม ในแต่ละ batch ต้องทำครบพร้อมกัน:

- QR และชื่อสื่อคู่บนใบงาน
- ระดับชั้น/ตัวชี้วัด/คำศัพท์ตรงกับสื่อหลัก
- เปลี่ยนกิจกรรม interactive ให้เป็น process scaffold สำหรับเขียนบนกระดาษ ไม่ลอกปุ่มหรือ quiz UI มาตรง ๆ
- ลงทะเบียนหมวด `worksheets` และเชื่อม `indicator_games` ใน migration เดียวกัน
- ตรวจไฟล์สื่อหลัก, metadata, shared asset version, scaffold และ A4 ผ่าน `verify:worksheet`

Batch 1: ข้อมูลและแผนภูมิ · ข้อเท็จจริง–ความคิดเห็น · Phonics · วัฏจักรน้ำ · อ่านฉลากโภชนาการ
