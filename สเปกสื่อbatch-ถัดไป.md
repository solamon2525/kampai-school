# สเปกออกแบบสื่อ — Batch ถัดไป (เต็มมิติ · สร้างได้เลย)

> อัปเดต: 2026-07-08  
> ต้นแบบคุณภาพข้อมูล: `thai-vocab-hub` (grade · indicator_code · emoji · relation keys · โหมดหลายแบบ) + `english/vocab-hub.html` (โหมดเล่นหนาแน่น)  
> ต้นแบบ UX สื่อ (ไม่ใช่เกม): `_template-media.html` · `digestive-system-media.html` · `food-chain-media.html`  
> **กฎร่วมทุกชิ้น:** `category_key='media'` · `tracked_game=false` · ไม่มีคะแนน/LB · ปก 📚 1280×720 · `KAMPAI.goHome()` · โปรเจคเตอร์เฟิร์ส

---

## 0. สัญญาคุณภาพข้อมูล (ทุกชิ้นต้องผ่าน)

เช่น `thai-vocab-hub` — **อย่าปล่อย payload บาง** ถ้าเนื้อหาแบบคลังคำ/การ์ด:

| มิติ | บังคับ | ตัวอย่าง |
|------|--------|----------|
| รหัสชิ้น | slug + path | `/games/thai/synonym-media.html` |
| ตัวชี้วัด | `indicator_code[]` 1–3 ตัว | `ท 1.1 ป.4/2` |
| ชั้น | `grade_levels[]` | `ป.3,ป.4` |
| หน่วยเนื้อหา | id · label · emoji · shortDesc | |
| ความสัมพันธ์ | groupId / pairId / parent-child | syn-group, หมู่–อาหาร |
| ตัวอย่างใช้จริง | sentenceTh และ/หรือ tip | ประโยค · สถานการณ์ |
| TTS cue | text ที่อ่านได้ชัด | คำ / ประโยคสั้น |
| เฉลยครู | explanation 1–2 ประโยค | โหมดฝึก |
| โหมด UI | ≥ 2 (เรียนรู้ + ฝึก) | ไม่บังคับแข่ง |

**โครงสร้างไฟล์แนะนำ 2 แบบ**

| แบบ | ใช้เมื่อ | โครง |
|-----|----------|------|
| **A — Single HTML** | ชิ้น interactive แผนภาพ/กริด/วงล้อ · ข้อมูล ≤ ~80 รายการ | `_template-media` + DATA inline |
| **B — Mini-hub** | คลังคำ/การ์ด ≥ 80 · หลายหมวด · filter ชั้น | โฟลเดอร์ `slug/` + `data/items/*.json` + `game.js` (เช่น thai-vocab-hub) |

ทุกชิ้นด้านล่างระบุชัดว่าใช้แบบ A หรือ B

---

## 1. T4 — ไวพจน์ / คำพ้องความหมาย (สื่อ)

| | |
|--|--|
| **รหัส** | T4 |
| **slug / path** | `thai/synonym-media.html` **(แบบ A)** หรือ `thai/synonym-hub/` **(แบบ B ถ้า ≥80 กลุ่ม)** |
| **ชั้น** | ป.4–6 |
| **ตัวชี้วัด** | `ท 1.1 ป.4/2` (เข้าใจความหมายคำ) · สนับสนุน `ท 4.1 ป.4/1` |
| **คู่เกม** | `waipot` / `wipod` |
| **ห้ามซ้ำ** | **อย่า copy 100 คำจาก** `thai-vocab-hub/data/words/synonyms.json` ทั้งก้อน — ชุดสอนสื่อ = กลุ่มคัดสรร + อธิบายการใช้ · ลิงก์ “ฝึกเพิ่มในคลังคำ” ไป vocab-hub |

### วัตถุประสงค์การสอน
นักเรียนเห็นว่า **คำหลายคำมีความหมายใกล้กัน** แต่ระดับภาษา / โทน / คู่ใช้ต่างกัน — เลือกคำให้เหมาะบริบท

### Schema หน่วยข้อมูล (1 กลุ่มไวพจน์)

```ts
type SynonymGroup = {
  id: string;                 // 'syn-sun'
  theme: string;              // 'ดวงอาทิตย์'
  emoji: string;
  grade: 'ป.4' | 'ป.5' | 'ป.6';
  indicator_code: string;     // ตัวหลักของกลุ่ม
  words: Array<{
    word: string;
    reading: string;          // คำอ่าน
    register: 'พูด' | 'เขียน' | 'กวี' | 'ราชา';
    nuance: string;           // ความต่างโทนสั้น ๆ
    example: string;          // ประโยคใช้จริง
  }>;
  tip: string;                // คำใบ้ครู 1 ประโยค
  quizWrong: string[];        // ตัวลวง (คำไม่ใช่ไวพจน์ของกลุ่มนี้)
};
```

### ชุดข้อมูลตั้งต้น (24 กลุ่ม · ≥ 72 คำ) — พอสำหรับสื่อ + โหมดฝึก

| id | theme | words (อย่างน้อย 3) | register เน้น |
|----|-------|---------------------|---------------|
| syn-sun | ดวงอาทิตย์ | ตะวัน, สุริยา, ทินกร | พูด / กวี / กวี |
| syn-moon | ดวงจันทร์ | เดือน, ศศิ, แข | พูด / กวี / กวี |
| syn-house | บ้าน | บ้าน, ที่อยู่อาศัย, สำนัก | พูด / เขียน / กวี |
| syn-child | เด็ก | เด็ก, หนูน้อย, กุมาร | พูด / พูด / เขียน |
| syn-good | ดี | ดี, เยี่ยม, ประเสริฐ | พูด / พูด / เขียน |
| syn-big | ใหญ่ | ใหญ่, มหา, กว้างใหญ่ | พูด / เขียน / เขียน |
| syn-small | เล็ก | เล็ก, น้อย, จิ๋ว | พูด / พูด / พูด |
| syn-fast | เร็ว | เร็ว, ไว, ฉับพลัน | พูด / พูด / เขียน |
| syn-slow | ช้า | ช้า, เชื่องช้า, เนิบนาบ | พูด / เขียน / เขียน |
| syn-happy | สุข | สุข, ยินดี, เบิกบาน | พูด / เขียน / เขียน |
| syn-sad | เศร้า | เศร้า, เสียใจ, โศก | พูด / พูด / เขียน |
| syn-see | ดู | ดู, มอง, เพ่ง | พูด / พูด / เขียน |
| syn-say | พูด | พูด, กล่าว, เอ่ย | พูด / เขียน / เขียน |
| syn-walk | เดิน | เดิน, ก้าว, ย่าง | พูด / พูด / กวี |
| syn-eat | กิน | กิน, รับประทาน, เสวย | พูด / เขียน / ราชา |
| syn-water | น้ำ | น้ำ, วารี, สายชล | พูด / กวี / กวี |
| syn-tree | ต้นไม้ | ต้นไม้, พฤกษา, พนาไพร | พูด / เขียน / กวี |
| syn-teacher | ครู | ครู, อาจารย์, ครูบา | พูด / เขียน / ท้องถิ่น |
| syn-friend | เพื่อน | เพื่อน, สหาย, มิตา | พูด / เขียน / กวี |
| syn-brave | กล้า | กล้า, หาญ, องอาจ | พูด / เขียน / เขียน |
| syn-smart | ฉลาด | ฉลาด, หลักแหลม, ปราดเปรื่อง | พูด / เขียน / เขียน |
| syn-beautiful | สวย | สวย, งาม, เลิศ | พูด / เขียน / เขียน |
| syn-help | ช่วย | ช่วย, สนับสนุน, อุปการะ | พูด / เขียน / เขียน |
| syn-end | จบ | จบ, สิ้นสุด, อวสาน | พูด / เขียน / เขียน |

*(ตอน implement: เติม `reading` · `nuance` · `example` · `quizWrong` ให้ครบทุกกลุ่ม — อย่าเหลือแค่ชื่อคำ)*

### โหมด UI (อย่างน้อย 4)

1. **เรียนรู้** — เลือกกลุ่ม → การ์ดคำเรียง · แตะเพื่อขยาย nuance + example · TTS คำ/ประโยค  
2. **แฟลชการ์ด** — หน้า: คำ · หลัง: ความหมายกลุ่ม + คำคู่ในกลุ่ม  
3. **เลือกไวพจน์** — โจทย์: “คำไหนพ้องความหมายกับ X?” (4 ตัวเลือก รวมตัวลวง)  
4. **จับคู่กลุ่ม** — ลากคำเข้ากล่องธีม (หรือแตะทีละคำแล้วเลือกกล่อง)  
5. *(optional)* **เลือกโทน** — “ประโยคนี้ควรใช้คำระดับใด: พูด/เขียน/กวี?”

### Footer / meta
`ตัวชี้วัด ท 1.1 ป.4/2 · คู่เกม waipot · ฝึกคลังเพิ่ม: thai-vocab-hub → synonyms`

---

## 2. H1 — อาหารหลัก 5 หมู่ (สื่อ)

| | |
|--|--|
| **รหัส** | H1 |
| **slug** | `health/food-groups-media.html` **(แบบ A)** |
| **ชั้น** | ป.3–4 |
| **ตัวชี้วัด** | `พ 4.1 ป.3/2` · `พ 4.1 ป.3/3` |
| **คู่เกม** | `plate-builder` |
| **ห้ามซ้ำ** | plate-builder = เกมคะแนน · สื่อนี้ = สาธิตหมู่ + จัดจานฝึกไร้คะแนน |

### Schema

```ts
type FoodGroup = {
  id: 1|2|3|4|5;
  nameTh: string;           // โปรตีน / คาร์โบไฮเดรต / ...
  shortName: string;        // ใช้บนจาน
  color: string;            // CSS token-friendly hex (ใช้ในสื่อเท่านั้น)
  role: string;             // ร่างกายได้อะไร
  tip: string;              // สัดส่วนง่าย ๆ
  emoji: string;
};

type FoodItem = {
  id: string;
  nameTh: string;
  emoji: string;
  groupId: 1|2|3|4|5;
  note?: string;            // เช่น ทอด = ไขมันเพิ่ม
  exampleMeal?: string;
};
```

### หมู่ 5 (บังคับ)

| id | nameTh | role | tip สัดส่วน |
|----|--------|------|-------------|
| 1 | โปรตีน | สร้างกล้ามเนื้อ ซ่อมแซม | 1 ส่วน / มื้อ |
| 2 | คาร์โบไฮเดรต | ให้พลังงานหลัก | 2–3 ส่วน / มื้อ |
| 3 | ผัก | วิตามิน ใยอาหาร | ให้มาก |
| 4 | ผลไม้ | วิตามิน หวานธรรมชาติ | 1–2 ส่วน / วัน |
| 5 | ไขมันดี | พลังงาน + ดูดซึมวิตามิน | น้อยแต่จำเป็น |

### ชุดอาหารตั้งต้น (≥ 30 รายการ)

**โปรตีน (1):** ไข่ · ไก่ · ปลา · หมู · เต้าหู้ · นม · กุ้ง · ถั่วลิสง  
**คาร์บ (2):** ข้าวสวย · ข้าวเหนียว · ก๋วยเตี๋ยว · ขนมปัง · เผือก · มันฝรั่ง · ข้าวโพด · โจ๊ก  
**ผัก (3):** ผักบุ้ง · คะน้า · แตงกวา · แครอท · ผักกาด · มะเขือเทศ · ถั่วฝักยาว · สลัด  
**ผลไม้ (4):** กล้วย · ส้ม · แอปเปิล · แตงโม · มะม่วง · ฝรั่ง · มะละกอ · องุ่น  
**ไขมัน (5):** น้ำมันพืช · อะโวคาโด · งา · เนย · ถั่วเปลือกแข็ง · กะทิ (เตือนใช้พอดี)

### โหมด UI
1. **เรียนรู้ 5 หมู่** — การ์ดหมู่ใหญ่ · แตะดูบทบาท + ตัวอย่างอาหาร  
2. **สำรวจอาหาร** — กริดอาหาร tap → ไฮไลต์หมู่  
3. **จัดจานตัวอย่าง** — เลือกอาหารให้ครบ 5 หมู่ (ไม่คะแนน · สุ่มโจทย์ “มื้อเช้า/กลางวัน”)  
4. **ถูก/ผิด** — “กล้วยอยู่ในหมู่ใด?”  
5. **เฉลยครู** — จานตัวอย่างสำเร็จรูป 3 จาน (เช้า/กลางวัน/เย็น)

### ลิงก์
`คู่เกม: /play/plate-builder`

---

## 3. H-body — กระดูก · กล้ามเนื้อ · ข้อ (สื่อ)

| | |
|--|--|
| **รหัส** | (แผน ★2 สุขศึกษา ป.4) |
| **slug** | `health/bone-muscle-media.html` **(แบบ A)** |
| **ชั้น** | ป.4–5 |
| **ตัวชี้วัด** | `พ 1.1 ป.4/2` · `พ 1.1 ป.4/3` |
| **เบสแพทเทิร์น** | `digestive-system-media.html` (แผนภาพคลิก + เรียงลำดับ) |
| **คู่เกม** | ไม่มีตรง — optional ลิงก์ `digestive-ar` เป็น “ระบบร่างกายอื่น” |

### Schema

```ts
type BodyPart = {
  id: string;                 // 'skull' | 'spine' | 'biceps' | 'knee' ...
  kind: 'bone' | 'muscle' | 'joint';
  nameTh: string;
  emoji: string;
  functionTh: string;         // หน้าที่
  careTip: string;            // ดูแลยังไง → ตรง พ 1.1 ป.4/3
  hotspot: { x: number; y: number }; // % บนภาพลำตัว
  facts: string[];            // 2–3 ข้อสั้น
};

type CareHabit = {
  id: string;
  label: string;
  good: boolean;
  why: string;
};
```

### เนื้อหาตั้งต้น — แผนภาพคลิก (≥ 12 hotspot)

**กระดูก (≥ 5):** กะโหลก · กระดูกสันหลัง · ซี่โครง · แขน (กระดูกต้นแขน) · ขา (โคนขา)  
**กล้ามเนื้อ (≥ 4):** กล้ามเนื้อต้นแขน · หน้าท้อง · ต้นขา · น่อง  
**ข้อ (≥ 3):** ข้อไหล่ · ข้อศอก · ข้อเข่า  

### CareHabit (≥ 8)
ดี: ดื่มนม/แคลเซียม · ออกกำลัง · ยืดเหยียด · ท่านั่งตรง · พักเมื่อเจ็บ  
ไม่ดี: นั่งงอหลังนาน · ยกของหนักผิดท่า · ไม่วอร์ม · กระโดดสูงซ้ำโดยไม่พัก

### โหมด UI
1. **เรียนรู้** — แตะ hotspot → การ์ดชื่อ/หน้าที่/เคล็ดดูแล  
2. **แยกประเภท** — ลาก/แตะจัดกล่อง กระดูก | กล้ามเนื้อ | ข้อ  
3. **ดูแลร่างกาย** — เลือกพฤติกรรมถูก/ผิด + คำอธิบาย  
4. **ทายตำแหน่ง** — ถาม “ข้อเข่าอยู่ตรงไหน?” แล้วแตะบนแผนภาพ

---

## 4. A1 — วงล้อสีวรรณะอุ่น–เย็น (สื่อ)

| | |
|--|--|
| **รหัส** | A1 |
| **slug** | `arts/color-wheel-media.html` **(แบบ A)** |
| **ชั้น** | ป.1–4 (เน้นป.4 วรรณะ) |
| **ตัวชี้วัด** | `ศ 1.1 ป.4/2` · `ศ 1.1 ป.4/7` · สนับสนุน `ศ 1.1 ป.4/9` |
| **คู่เกม** | `arts/color-wheel/` (เกม explore+quiz มีคะแนน/ชีวิต) |
| **ห้ามซ้ำ** | เกมมี 12 สี + quiz 10 ข้อแล้ว — สื่อเพิ่ม **อารมณ์วรรณะ · ตัวอย่างงานศิลป์ · เลือกสีระบายฉาก** โดยไม่มี lives |

### Schema

```ts
type WheelColor = {
  id: number;                 // 0–11
  nameTh: string;
  nameEn: string;
  hex: string;
  hue: number;
  level: 1|2|3;               // แม่สี / ขั้น 2 / ขั้น 3
  tone: 'warm' | 'cool' | 'both';
  emotion: string;            // อารมณ์ที่สื่อ
  exampleScene: string;       // ใช้ระบายฉากอะไร
};

type MoodScene = {
  id: string;
  title: string;              // 'พระอาทิตย์ตก' | 'ใต้ทะเล' ...
  recommendedTone: 'warm' | 'cool';
  why: string;
  samplePalette: number[];    // ids จาก wheel
};
```

### ข้อมูลสี 12 วง (reuse จาก `color-wheel/data.js` แล้วเสริม emotion/exampleScene)

แดง · ส้มแดง · ส้ม · ส้มเหลือง · เหลือง · เขียวเหลือง · เขียว · เขียวน้ำเงิน · น้ำเงิน · ม่วงน้ำเงิน · ม่วง · ม่วงแดง  

**วรรณะอุ่น:** แดง–ส้ม–เหลือง (+ ม่วงแดงบางโทน)  
**วรรณะเย็น:** เขียว–น้ำเงิน–ม่วง  

### MoodScene (≥ 6)
พระอาทิตย์ตก (อุ่น) · ไฟแคมป์ (อุ่น) · ทุ่งหญ้าเช้า (ผสม) · ใต้ทะเล (เย็น) · ฝนตกบนหน้าต่าง (เย็น) · งานเลี้ยงวันเกิด (อุ่นสดใส)

### โหมด UI
1. **วงล้อสำรวจ** — แตะสี → ชื่อ · วรรณะ · อารมณ์ · TTS ชื่อสี  
2. **อุ่น vs เย็น** — ไฮไลต์ครึ่งวง + คำอธิบายอิทธิพลต่ออารมณ์ (`ศ 1.1 ป.4/2`)  
3. **จัดฉาก** — เลือกฉาก → เลือกวรรณะ/จานสีที่เหมาะ · เฉลยคำอธิบาย  
4. **ผสมแม่สี** — แดง+เหลือง=ส้ม ฯลฯ (สาธิต)  
5. **ถูก/ผิด** — “สีน้ำเงินให้ความรู้สึกวรรณะใด?”

---

## 5. C1 — อาชีพในชุมชน (สื่อ)

| | |
|--|--|
| **รหัส** | C1 |
| **slug** | `career/community-jobs-media.html` **(แบบ A)** |
| **ชั้น** | ป.1–4 (เน้น `ง 2.1 ป.4/1`) |
| **ตัวชี้วัด** | `ง 2.1 ป.4/1` · ขยาย `ง 2.1 ป.5/1` ได้ |
| **คู่เกม** | `veggie-garden` (บริบทชุมชน/เกษตร) |
| **เสริม** | EN vocab-hub มี `jobs` 12 คำ — สื่อนี้เป็น **อาชีพไทยชุมชนชนบท** + ความหมายงาน |

### Schema

```ts
type Job = {
  id: string;
  nameTh: string;
  emoji: string;
  sector: 'เกษตร' | 'บริการ' | 'ค้าขาย' | 'ช่าง' | 'สาธารณสุข' | 'การศึกษา' | 'ความปลอดภัย';
  place: string;              // ที่ทำงาน
  duty: string;               // ทำอะไร
  tools: string[];            // ของใช้
  skill: string;              // คุณสมบัติ
  whyMatters: string;         // สำคัญต่อชุมชนอย่างไร
  grade: 'ป.1'|'ป.2'|'ป.3'|'ป.4';
};
```

### ชุดอาชีพตั้งต้น (≥ 16)

| sector | อาชีพ |
|--------|--------|
| เกษตร | ชาวนา · ชาวสวน · คนเลี้ยงสัตว์ · คนประมง |
| บริการ | คนขับรถรับจ้าง · พนักงานร้านค้า · ช่างตัดผม |
| ค้าขาย | พ่อค้าตลาด · แม่ค้าขายของชำ |
| ช่าง | ช่างไม้ · ช่างซ่อมรถ · ช่างไฟ |
| สาธารณสุข | หมอ · พยาบาล · อสม. |
| การศึกษา | ครู |
| ความปลอดภัย | ตำรวจ · อสม./อาสา (แยกจากหมอได้) · ทหาร (optional) |

*(ปรับใหครบ 16+ โดยไม่ซ้ำชื่อ)*

### โหมด UI
1. **การ์ดอาชีพ** — พลิก: หน้า emoji+ชื่อ · หลัง หน้าที่/เครื่องมือ  
2. **จัดกลุ่มภาค** — ลากเข้ากล่องประเภท  
3. **ใครทำอะไร** — จับคู่ อาชีพ ↔ สถานที่ / เครื่องมือ  
4. **สำคัญต่อชุมชน** — สถานการณ์สั้น “ถ้าไม่มีครู…?” เลือกผลกระทบ  
5. **สำรวจตัวเอง** *(ป.4+)* — “ชอบทำอะไร → อาชีพที่ใกล้เคียง” (ไม่ตัดสินถูกผิด)

---

## 6. M-area — พื้นที่สี่เหลี่ยมมุมฉาก (สื่อ)

| | |
|--|--|
| **รหัส** | (คิวป.4 คณิต #6) |
| **slug** | `math/rect-area-media.html` **(แบบ A)** |
| **ชั้น** | ป.4 |
| **ตัวชี้วัด** | `ค 2.1 ป.4/3` |
| **คู่เกม** | `mini-farm-island` (เกม — คนละประสบการณ์) |
| **กฎ** | ไม่จับเวลา · สูตร `กว้าง × ยาว` · กริดนับช่องชัดบนโปรเจคเตอร์ |

### Schema

```ts
type GridProblem = {
  id: string;
  width: number;              // หน่วยช่อง
  height: number;
  unit: 'ช่อง' | 'ซม.' | 'ม.';
  context: string;            // 'สวนผัก' | 'ห้องเรียน' ...
  showFormula: boolean;
  distractors?: number[];     // คำตอบลวงโหมดฝึก = ก+ย หรือ ก×2
};
```

### ชุดโจทย์ตั้งต้น (≥ 20)
- กริดเล็ก: 2×3, 3×3, 3×4, 4×5, 5×5, 2×8, 6×3 …  
- โจทย์ข้อความ: สวนกว้าง 4 ม. ยาว 6 ม. · พื้นกระเบื้อง 5×3 · ผ้าหน้าโต๊ะ 2×7  
- กับดัก: สับสนเส้นรอบรูป — มีโหมด “นี่พื้นที่หรือเส้นรอบรูป?”

### โหมด UI
1. **เรียนรู้** — กริดโต้ตอบ ลากปรับ ก×ย · นับช่องไฮไลต์ · โชว์สูตร  
2. **นับช่อง** — กริดสุ่ม · นักเรียนกรอกพื้นที่ · เฉลยทีละขั้น  
3. **สูตร** — ใส่ ก และ ย → คำนวณ  
4. **โจทย์เรื่อง** — อ่านโจทย์สั้น · เลือกคำตอบ  
5. **เส้นรอบรูป vs พื้นที่** — สองคำถามสลับกันให้แยกมโนทัศน์

### สูตรแสดงครู
`พื้นที่ = กว้าง × ยาว` · `เส้นรอบรูป = 2 × (กว้าง + ยาว)` (เทียบให้เห็นความต่าง)

---

## 7. T-dict — พจนานุกรมดิจิทัล (สื่อ)

| | |
|--|--|
| **รหัส** | (แผนไทย ★2) |
| **slug** | `thai/dictionary-media.html` **(แบบ B แนะนำ)** โฟลเดอร์เล็กได้ |
| **ชั้น** | ป.3–4 |
| **ตัวชี้วัด** | `ท 4.1 ป.3/3` · `ท 4.1 ป.4/3` |
| **คู่เกม** | ไม่มีตรง — ใช้เป็น skill tool |
| **ห้ามซ้ำ** | ไม่ใช่คลังคำทั้งหมดของ vocab-hub — เป็น **สอนวิธีกินพจนานุกรม** + คำตัวอย่างฝึกค้น |

### Schema

```ts
type DictEntry = {
  id: string;
  headword: string;           // คำมอง
  reading: string;
  partOfSpeech: 'น.'|'ก.'|'ว.'|'บุพบท'|...;
  meanings: Array<{
    def: string;
    example: string;
  }>;
  synonyms?: string[];
  antonyms?: string[];
  note?: string;              // คำคม/ระวังสะกด
  guideLetter: string;        // ก ข ค … สำหรับเลียนพจนานุกรม
  grade: string;
  emoji?: string;
};
```

### เนื้อหาสอน (แผงซ้ายถาวร)
1. เรียงตามตัวอักษร  
2. ดูคำอ่าน  
3. อ่านชนิดคำ  
4. อ่านความหมาย + ตัวอย่าง  
5. ดูคำพ้อง/คำตรงข้าม (ถ้ามี)

### ชุดคำตั้งต้น (≥ 40 คำ — คัดให้ครบ ก–ฮ บางส่วน)
กระจายพยัญชนะต้น + คำที่มักเปิดพจนานุกรมผิด (เช่น กะเพรา/กระเพรา ใน note)  
รวมคำเชื่อมบทเรียนอื่น: พื้นที่ · วรรณะ · พอเพียง · ราก (พืช) เป็นต้น — สร้างสะพานข้ามวิชา

### โหมด UI
1. **สาธิต** — แอนิเมชัน/ขั้นตอน 5 ข้อข้างบน  
2. **ค้นหา** — ช่องค้น + ตัวอักษรนำ (ก ข ค…)  
3. **ฝึกเปิด** — โจทย์: “หาความหมายของคำว่า …” แล้วเลือกข้อถูก  
4. **จัดเรียง** — เรียง 5 คำตามพจนานุกรม  
5. **อ่านบทความ** — ประโยคมีคำขีดเส้นใต้ → แตะเปิดนิยามจำลอง

---

## 8. S-plant — ส่วนพืชดอก (สื่อ)

| | |
|--|--|
| **รหัส** | (วิทย์ ป.4 ★3) |
| **slug** | `science/plant-parts-media.html` **(แบบ A)** |
| **ชั้น** | ป.4 |
| **ตัวชี้วัด** | `ว 1.2 ป.4/1` |
| **คู่เกม** | `veggie-garden` |
| **แพทเทิร์น** | digest media + water-cycle (คลิกส่วน + เรียงหน้าที่) |

### Schema

```ts
type PlantPart = {
  id: 'root'|'stem'|'leaf'|'flower'|'fruit'|'seed';
  nameTh: string;
  emoji: string;
  functionTh: string;
  funFact: string;
  hotspot: { x: number; y: number };
  examples: string[];         // แครอท=ราก, อ้อย=ลำต้น ...
};

type PlantExample = {
  id: string;
  nameTh: string;
  emoji: string;
  ediblePart: PlantPart['id'];
  tip: string;
};
```

### ส่วนบังคับ (อย่างน้อย 4 ตามตัวชี้วัด + เสริมผล/เมล็ด)

| ส่วน | หน้าที่สั้น |
|------|-------------|
| ราก | ดูดน้ำ/แร่ · ยึดลำต้น |
| ลำต้น | ลำเลียง · ค้ำ |
| ใบ | สังเคราะห์แสง |
| ดอก | สืบพันธุ์ · ดึงแมลง |
| ผล *(เสริม)* | หุ้มเมล็ด |
| เมล็ด *(เสริม)* | ต้นใหม่ |

### PlantExample (≥ 12)
แครอท(ราก) · มันสำปะหลัง(ราก) · อ้อย(ลำต้น) · ผักบุ้ง(ใบ+ลำต้น) · กะหล่ำปลี(ใบ) · กุหลาบ(ดอก) · มะม่วง(ผล) · ข้าวโพด(เมล็ด) · ถั่วลันเตา(ผล/เมล็ด) · หอมแดง(ลำต้นสะสมอาหาร — ระบุใน tip) · มันฝรั่ง(ลำต้นใต้ดิน — tip) · แตงกวา(ผล)

### โหมด UI
1. **แผนภาพคลิก** — แตะส่วนพืช → หน้าที่ + TTS  
2. **จับคู่หน้าที่** — การ์ดหน้าที่ → ส่วน  
3. **กินส่วนไหน** — อาหารตัวอย่าง → เลือกส่วนที่กิน  
4. **เรียงการเติบโต** *(optional)* — เมล็ด→ต้นอ่อน→ต้นโต→ดอก→ผล  

---

## 9. S-moon — ดวงจันทร์ 8 ข้าง (สื่อ)

| | |
|--|--|
| **รหัส** | (วิทย์ ป.4) |
| **slug** | `science/moon-phases-media.html` **(แบบ A)** |
| **ชั้น** | ป.4 |
| **ตัวชี้วัด** | `ว 3.1 ป.4/1` · `ว 3.1 ป.4/2` |
| **แพทเทิร์น** | A (ขั้นตอนหมุนได้) เช่น water-cycle |

### Schema

```ts
type MoonPhase = {
  id: string;
  order: 1|2|3|4|5|6|7|8;
  nameTh: string;             // ข้างจันทร์ใหม่ ... ข้างแรม
  nameShort: string;
  emoji: string;              // หรือ SVG path lit fraction
  litFraction: number;        // 0–1
  waxing: boolean | null;     // true = ข้างขึ้น, false = ข้างแรม, null = ใหม่/เต็มดวง
  dayApprox: string;          // '~วันที่ 1 ของจันทรคติ'
  tip: string;
  observeHint: string;        // มองท้องฟ้าอย่างไร
};
```

### 8 ข้าง (ลำดับมาตรฐาน)

1. จันทร์ใหม่ (New) · lit 0  
2. พระจันทร์เสี้ยวข้างขึ้น (Waxing Crescent)  
3. จันทร์ครึ่งซีกข้างขึ้น (First Quarter)  
4. จันทร์เกือบบรรจบข้างขึ้น (Waxing Gibbous)  
5. จันทร์เต็มดวง (Full) · lit 1  
6. จันทร์เกือบบรรจบข้างแรม (Waning Gibbous)  
7. จันทร์ครึ่งซีกข้างแรม (Last Quarter)  
8. พระจันทร์เสี้ยวข้างแรม (Waning Crescent)  

### โหมด UI
1. **วงโคจรจำลอง** — โลก–จันทร์–ดวงอาทิตย์ หมุนช้า · ไฮไลต์ข้างปัจจุบัน *(SVG เบา ไม่ต้อง 3D)*  
2. **ไทม์ไลน์ 8 ข้าง** — แตะทีละขั้น · TTS ชื่อ  
3. **เรียงลำดับ** — สลับการ์ด 8 ใบให้ถูกลำดับ  
4. **ทายข้าง** — โชว์เงา → เลือกชื่อ  
5. **พยากรณ์** — “ถัดจากเต็มดวงคือข้างใด?” (`ว 3.1 ป.4/2`)

### หมายเหตุครู
อธิบายสั้น: เราเห็นส่วนที่โดนแสงอาทิตย์ · จันทร์ไม่ได้มีแสงเอง

---

## 10. O-suff — เศรษฐกิจพอเพียง (สื่อ)

| | |
|--|--|
| **รหัส** | (สังคม ป.4) |
| **slug** | `social/sufficiency-media.html` **(แบบ A)** |
| **ชั้น** | ป.4–6 |
| **ตัวชี้วัด** | `ส 3.1 ป.4/3` |
| **รูปแบบ** | การ์ดหลักการ + สถานการณ์เลือก |

### Schema

```ts
type Pillar = {
  id: 'moderation'|'reasonableness'|'immunity';
  nameTh: string;             // ความพอประมาณ · มีเหตุผล · มีภูมิคุ้มกัน
  emoji: string;
  meaning: string;
  kidExample: string;
};

type Condition = {
  id: 'knowledge'|'virtue';
  nameTh: string;             // เงื่อนไขความรู้ · เงื่อนไขคุณธรรม
  meaning: string;
};

type Scenario = {
  id: string;
  situation: string;
  choices: Array<{
    text: string;
    ok: boolean;
    pillar?: Pillar['id'];
    why: string;
  }>;
};
```

### เนื้อหาหลัก (บังคับครบ)

**3 ห่วง**
1. **พอประมาณ** — ไม่ฟุ่มเฟือย ไม่ขาดแคลนเกินไป  
2. **มีเหตุผล** — ตัดสินใจโดยพิจารณาเหตุผลและผลกระทบ  
3. **มีภูมิคุ้มกัน** — พร้อมรับความเปลี่ยนแปลง / มีเงินออม สำรอง

**2 เงื่อนไข**
- ความรู้  
- คุณธรรม (ซื่อสัตย์ ขยัน อดทน แบ่งปัน)

### Scenario (≥ 10 สถานการณ์เด็กประถม)
เช่น: ได้เงินค่าขนม · อยากได้ของเล่นแพง · เก็บออม · ช่วยงานบ้าน · ใช้น้ำไฟ · แบ่งปันเพื่อน · ทำสวนครัว · ซื้อของโดยเปรียบเทียบราคา · ยืมเงินเพื่อน · ทิ้งขยะ

### โหมด UI
1. **เรียนรู้ 3 ห่วง 2 เงื่อนไข** — แผนภาพ + แตะการ์ด  
2. **จับคู่** — คำอธิบาย → ชื่อหลักการ  
3. **สถานการณ์** — เลือกคำตอบที่มีเหตุผล + เฉลยว่าตรงห่วงใด  
4. **แผนของฉัน** — กรอก/เลือก “สัปดาห์นี้ฉันจะพอเพียงด้าน…” (บันทึก localStorage ได้ ไม่ต้องเซิร์ฟเวอร์)

---

## 11. สรุปสเปกข้ามชิ้น (เปรียบเทียบ)

| ชิ้น | slug | แบบ | โหมดขั้นต่ำ | หน่วยข้อมูลขั้นต่ำ | ตัวชี้วัดหลัก |
|------|------|-----|------------|-------------------|---------------|
| T4 ไวพจน์ | `synonym-media` | A/B | 4 | 24 กลุ่ม | ท 1.1 ป.4/2 |
| H1 อาหาร 5 หมู่ | `food-groups-media` | A | 4 | 5 หมู่ + 30 อาหาร | พ 4.1 ป.3/2–3 |
| กระดูก/กล้ามเนื้อ | `bone-muscle-media` | A | 4 | 12 hotspot + 8 นิสัย | พ 1.1 ป.4/2–3 |
| A1 วงล้อสี | `color-wheel-media` | A | 4 | 12 สี + 6 ฉาก | ศ 1.1 ป.4/2,/7 |
| C1 อาชีพ | `community-jobs-media` | A | 4 | 16 อาชีพ | ง 2.1 ป.4/1 |
| พื้นที่สี่เหลี่ยม | `rect-area-media` | A | 4 | 20 โจทย์ | ค 2.1 ป.4/3 |
| พจนานุกรม | `dictionary-media` | B | 4 | 40 คำ + คู่มือ 5 ขั้น | ท 4.1 ป.3/3,/4/3 |
| ส่วนพืช | `plant-parts-media` | A | 4 | 6 ส่วน + 12 ตัวอย่าง | ว 1.2 ป.4/1 |
| ดวงจันทร์ | `moon-phases-media` | A | 4 | 8 ข้าง | ว 3.1 ป.4/1–2 |
| เศรษฐกิจพอเพียง | `sufficiency-media` | A | 4 | 3+2 + 10 scenario | ส 3.1 ป.4/3 |

---

## 12. Definition of Done (ทุกชิ้น)

1. HTML จาก `_template-media` (หรือ hub scaffold) + `kampai-sdk.js` + `setSlug`  
2. ไม่มี `submitScore` / lives / LB  
3. โหมดเรียนรู้ + ฝึก + เฉลย  
4. ข้อมูลครบมิติตาม schema (ไม่ใช่แค่ชื่อรายการ)  
5. Migration seed `media` + thumbnail 📚 1280×720 + tags/grade/subject  
6. `indicator_games` map + `game_docs`  
7. `pnpm verify:media <path>` ผ่าน checks 1,3,7,9  
8. Footer ระบุตัวชี้วัด + ลิงก์คู่เกม (ถ้ามี)  
9. อัปเดต `แผนสื่อตามตัวชี้วัด.md` · `ไอเดียทำสื่อ.md` · คิวใน `แผนพัฒนาคลังสื่อ.md`

---

## 13. ลำดับ implement ที่แนะนำ (หลังมีสเปกนี้)

```
Batch X1 (สาระว่าง + คู่เกมพร้อม):
  1) food-groups-media     (คู่ plate-builder)
  2) color-wheel-media     (คู่ color-wheel)
  3) synonym-media         (คู่ waipot — อย่ากลืนกิน vocab-hub)

Batch X2 (ป.4 ตรงตัวชี้วัด):
  4) plant-parts-media
  5) moon-phases-media
  6) rect-area-media

Batch X3 (ปิดช่องว่างวิชา):
  7) bone-muscle-media
  8) community-jobs-media
  9) sufficiency-media
 10) dictionary-media
```

---

## 14. กันสับสนกับของที่มีแล้ว

| ของเดิม | บทบาท | สื่อใหม่ต้อง |
|---------|--------|-------------|
| `thai-vocab-hub` synonyms | คลัง 100 คำฝึกภาษา | สอนแนวคิดไวพจน์ + กลุ่มคัด · ลิงก์ไปคลัง |
| `waipot`/`wipod` | เกมกระดานคะแนน | คู่ลิงก์เท่านั้น |
| `plate-builder` | เกมจัดจานคะแนน | สาธิตหมู่ + ฝึกไร้คะแนน |
| `color-wheel` game | quiz มีชีวิต | วรรณะอุ่น–เย็น + อารมณ์ |
| `digestive-system-media` | แผนภาพร่างกาย | แบบอย่าง UX ของ bone-muscle |
| `mini-farm-island` | เกมพื้นที่ | สื่อกริด+สูตรคนละไฟล์ |
| Savings Bank app | แอปออมเงิน | ไม่ใช่สื่อพอเพียง — คนละบริบท |

---

## 15. Checklist ตอนสั่ง AI สร้างโค้ด (แปะใน prompt)

```
สร้างสื่อการสอนตามสเปกใน สเปกสื่อbatch-ถัดไป.md ส่วน [รหัส]
- อ่าน MEDIA-PROMPT.md + _template-media.html
- ใส่ DATA ครบ schema (อย่าย่อ remaining เป็น TODO)
- โหมดครบตามสเปก
- ภาษาไทย UI, Sarabun, light mode CSS vars / ไม่ dark:
- KAMPAI.setSlug + goHome ปุ่มกลับ
- tracked_game=false ใน seed
```

---

> เอกสารนี้พร้อมใช้เป็น blueprint ตอนลงมือเขียน HTML / migration  
> เมื่อเริ่ม Batch X1 ให้แจ้งชิ้น — จะลงโค้ดเต็มตามสเปกนี้ได้ทันที
