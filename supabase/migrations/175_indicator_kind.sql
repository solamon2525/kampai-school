-- ============================================================================
-- Migration 175: เพิ่มมิติ "ตัวชี้วัดระหว่างทาง / ปลายทาง" (เอกสาร สพฐ. ปรับปรุง)
-- ============================================================================
-- curriculum_indicators (mig 170) ยังไม่มีการแยกประเภทตัวชี้วัดตามเอกสารทางการ
-- "ตัวชี้วัดระหว่างทางและตัวชี้วัดปลายทาง ตามหลักสูตรแกนกลางฯ 2551"
--   • ระหว่างทาง = formative (ประเมินระหว่างเรียน)
--   • ปลายทาง    = summative (ประเมินปลายภาค/ตัวชี้วัดหลัก)
-- nullable: ตัวชี้วัดเดิม (seed ไทยก่อนหน้า) เป็น NULL จนกว่าจะ re-seed พร้อม kind
-- ============================================================================

ALTER TABLE public.curriculum_indicators
  ADD COLUMN IF NOT EXISTS indicator_kind TEXT
  CHECK (indicator_kind IN ('ระหว่างทาง', 'ปลายทาง'));
